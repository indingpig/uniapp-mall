# 数据采集指南 - Edge Impulse 婴儿哭闹检测

> 本文档为 Edge Impulse 训练数据采集提供指导，用于部署到 ESP32 的设备端哭闹检测。

## 1. 类别设计（5 类）

| 类别 | 定义 | 示例 |
|------|------|------|
| `crying` | 婴儿哭闹 | 不同哭法、不同距离、床头/房门口 |
| `babble` | 宝宝咿呀发声 | 翻动哼唧、不同音量 |
| `speech` | 父母说话 | 对话、轻声、正常音量 |
| `noise` | 明确声源的机械/环境噪声 | 键盘、空调、家电、碰撞、窗外 |
| `quiet` | 安静环境底噪 | 纯静音、白噪声、轻微底噪 |

## 2. 类别边界判断

- **quiet vs noise**：声音大到会想"什么东西在响"就是 noise；"听不太清但隐约有"是 quiet
- **babble vs quiet**：明显连续发声是 babble；偶发单音节归 quiet
- **键盘声**：机械键盘/笔记本键盘敲击归 **noise**（若不加 noise 类则归 quiet）

## 3. 录音参数

| 参数 | 建议值 |
|------|--------|
| 采样率 | 16000 Hz（与固件 I2S 一致） |
| 窗口尺寸 | 1000 ms |
| 步长 | 500 ms（滑动窗口增广） |
| 量化 | int8（ESP32 推理提速） |

## 4. 各类样本量（推荐目标）

| 类别 | 片段数 | 每段时长 | 总时长 | 录制场景覆盖 |
|------|--------|---------|--------|-------------|
| crying | 100-120 | 5s | 8-10 分钟 | 不同哭法、不同距离、床头/房门口、哭闹起伏 |
| babble | 80-100 | 5s | 6-8 分钟 | 咿呀、翻动哼唧、不同音量 |
| speech | 80-100 | 5s | 6-8 分钟 | 爸妈对话、轻声、正常音量、不同房间位置 |
| noise | 80-100 | 5s | 6-8 分钟 | 见下方清单 |
| quiet | 100-140 | 5s | 10-12 分钟 | 纯静音、白噪声、轻微底噪、混合环境 |

**总计：约 440-560 段，35-45 分钟音频**

> **为什么是这个量级：** 5 分钟是底线，10 分钟以上才稳健。
> - 假设 1s 窗口 + 0.5s 步长，8 分钟 ≈ 480 个训练窗口/类，起步才够
> - `quiet`/`noise` 是背景类，**样本越多误报越少**（把底噪误判成哭闹是主要风险）
> - 比起总时长，**场景多样性更重要**——同一地点录 30 分钟不如 3 个地点各录 10 分钟
> - 数据量不足的补救：可加降噪/音量抖动/时移等数据增广，但远不如真数据可靠

## 5. noise 类录制清单

- 键盘声：机械键盘打字/回车/空格、薄膜/笔记本键盘、连续 vs 零星
- 家电：空调/风扇（近+远）、冰箱压缩机、净化器、加湿器、吸尘器、吹风机
- 电子设备：手机震动/消息提示音、电视/电脑低声量、耳机漏音
- 家居碰撞：门开关、抽屉/柜子、物品掉落、拍打、杯子碰撞、玩具自动发声
- 环境：窗外车流/雨/风、楼上楼下脚步、装修（远处）
- 宠物（如有）：猫叫、狗叫、鸟叫

## 6. 录制要点

- 每个噪声源 10-20 秒，切成小段
- 同一场景多录几段，避免过拟合
- crying 包含"由弱到强"过渡段，模拟真实哭闹
- 每类录完立即试听，剔除叠加噪声的段
- 在宝宝睡觉的实际房间录，贴近真实场景

## 7. Edge Impulse 配置建议

| 参数 | 建议值 |
|------|--------|
| 采样率 | 16000 Hz |
| 特征 | **MFE（梅尔滤波器组能量）** |
| 数据划分 | 80% train / 20% test |
| 模型 | 轻量分类网络（DSCNN 或 4-6 层卷积） |
| 目标 RAM | < 150KB |
| 量化 | int8 |

### 特征选择：MFE 优于 MFCC

| 维度 | MFE（推荐） | MFCC |
|------|------------|------|
| 特征形式 | log 梅尔频谱带能量（2D 图像） | MFE 再做 DCT 变换 |
| 信息量 | 保留完整频谱细节 | DCT 去冗余，丢一部分信息 |
| 对 CNN | 更友好，网络自行提取特征 | 经典语音识别用（HMM 时代） |
| 计算量 | 少一步 DCT，ESP32 上更快 | 多一步 DCT |
| 噪声鲁棒 | 一般 | 传统上更好（CNN 下差异小） |

**为什么选 MFE：**
- CNN 会自行从频谱图提取特征，DCT 的压缩可能丢掉哭声关键频段信息
- 婴儿哭声高频细节（3-8kHz 音高起伏）在 MFE 里保留更完整
- ESP32 上少一步 DCT，推理更快
- Edge Impulse 音频分类官方模板默认 MFE + DSCNN

**例外：** 若需在 ESP32 极限压缩 RAM，MFCC 维度低可省内存，但准确率可能略降。建议直接用 MFE。

## 8. 采样一致性（重要）

- 有条件用 **Edge Impulse CLI + ESP32 直接采集**，保证训练数据与部署设备（INMP441 麦克风）同源
- 若用手机录音训练，频谱与 INMP441 有差异，可能影响真机准确率

## 9. 训练后导出

- 导出类型：**Arduino Library**
- 记录导出后的 `EI_CLASSIFIER_LABEL_COUNT` 和标签数组（类别顺序），用于固件集成
